# HTTP
aiohttp>=3.9
requests>=2.32          # kept for sync fallback in setup.sh only

# HTML parsing
beautifulsoup4>=4.12
lxml>=5.0

# PostgreSQL (psycopg3 — NOT psycopg2)
psycopg[binary]>=3.1

# Redis
redis>=5.0

# Vector embeddings + Ollama
ollama>=0.3

# Date parsing
python-dateutil>=2.9

# Token counting (for long-context detection)
tiktoken>=0.7

# Analytics (v1: direct PG queries; v2: DuckDB + postgres_scanner)
duckdb>=1.0
pandas>=2.0

# CLI + terminal output
typer>=0.12
rich>=13.0

# Cloud sync
google-cloud-storage>=2.0     # optional: only needed if using gcloud auth

# Markdown / data export
# (no extra deps — built-in csv, json)

# v2 only (do not install for v1):
# owlready2>=0.46
# rdflib>=7.0
